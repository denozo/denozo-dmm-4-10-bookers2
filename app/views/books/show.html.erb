<div class="container">
 <div class="row">
   
  <div class="col-lg-4">
    
  </div> 
   
  <div class="col-lg-8">
    <h1>Book detail</h1>
    <table class="table table-hover">
      <thead>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
      </thead>
      <tbody>
      <% @books.each do |book| %>
        <tr>
          <td><%= link_to book.title, book_path(book.id), class: 'show' %></td>
          <td><%= book.body %></td>
          <td><%= link_to 'Edit', edit_book_path(book.id),class: 'tn btn-sm btn-success edit' %></td>
          <td><%= link_to 'Destroy', book_path(book.id), class: 'btn btn-sm btn-danger Destroy', method: :delete, "data-confirm" => "Are you sure？" %></td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>

 </div>
</div>